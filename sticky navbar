window.onscroll = function() {scrollNow()};
var theNavbar = document.getElementById("navbar");
var sticky = theNavbar.offsetTop;

function scrollNow() {
  if (window.pageYOffset >= sticky) {
    theNavbar.classList.add("sticky")
  } else {
    theNavbar.classList.remove("sticky");
  }
}
